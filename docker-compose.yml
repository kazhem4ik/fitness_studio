services:
  bot_service:
    build:
      context: ./bot_service
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "8000"
    volumes:
      - .:/app
      - ./database:/database
    environment:
      - PYTHONPATH=/app
    env_file:
      - .env
    command: uvicorn bot_service.main:app --host 0.0.0.0 --port 8000

  planner_service:
    build:
      context: ./planner_service
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "8001"
    volumes:
      - .:/app
      - ./database:/database
    environment:
      - PYTHONPATH=/app
    env_file:
      - .env
    command: uvicorn planner_service.main:app --host 0.0.0.0 --port 8001

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:80"
    volumes:
      - ./landing_page:/usr/share/nginx/html/landing:ro
      - ./planner_service/static:/usr/share/nginx/html/planner:ro
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - bot_service
      - planner_service
